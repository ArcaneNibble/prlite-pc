obstacle_range: 2.5
raytrace_range: 3.0
footprint: [[-0.4, 0.25], [0.4, 0.25], [0.4, -0.25], [-0.4, -0.25]] # hopefully I don't have x and y switched here
#robot_radius: ir_of_robot # use this instead of footprint if robot is circular
inflation_radius: 0.55

observation_sources: laser_scan_sensor #point_cloud_sensor

laser_scan_sensor: {sensor_frame: base_link, data_type: LaserScan, topic: /scan, marking: true, clearing: true}

#point_cloud_sensor: {sensor_frame: frame_name, data_type: PointCloud, topic: topic_name, marking: true, clearing: true}
